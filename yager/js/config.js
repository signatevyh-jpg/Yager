/**
 * ЕДИНАЯ ТОЧКА ПОДКЛЮЧЕНИЯ БЭКЕНДА
 * --------------------------------
 * Сейчас USE_MOCK = true, поэтому api.js хранит всё в localStorage
 * этого браузера (у каждого пользователя — своя изолированная копия,
 * реального обмена сообщениями между людьми нет).
 *
 * Когда бэкенд будет готов:
 *   1. Поставьте USE_MOCK = false
 *   2. Укажите API_BASE_URL и WS_URL вашего сервера
 *   3. Реализуйте на сервере эндпоинты, перечисленные в api.js
 *      (контракт запросов/ответов уже описан в комментариях там же)
 * Остальной код (auth.js, app.js) трогать не нужно — он обращается
 * только к функциям объекта PoyetAPI, а не напрямую к localStorage/fetch.
 */
const CONFIG = {
  USE_MOCK: false,
  API_BASE_URL: '',
  WS_URL: `${window.location.protocol === 'https:' ? 'wss:' : 'ws:'}//${window.location.host}/ws`,
};
